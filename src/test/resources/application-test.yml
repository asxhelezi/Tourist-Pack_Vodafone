spring:
  datasource:
    url: jdbc:h2:mem:touristpack;DB_CLOSE_DELAY=-1
    driver-class-name: org.h2.Driver
    username: sa
    password: ""
  jpa:
    hibernate:
      ddl-auto: create-drop
  mail:
    host: localhost
    port: 3025

payment:
  mock-enabled: true

mail-sending:
  mock-enabled: true

redemption:
  expiry-days: 30

app:
  frontend-base-url: http://localhost:3000
  qr-storage-dir: ./build/test-qrcodes
